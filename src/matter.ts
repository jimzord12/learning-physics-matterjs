// import { Engine, Render, Runner, Bodies, Composite, Body } from 'matter-js';

// const { rectangle } = Bodies;
// const { setAngularVelocity } = Body;

// const engine = Engine.create();

// const boxA = rectangle(400, 200, 80, 80);
// const boxB = rectangle(450, 50, 80, 80);
// const ground = rectangle(400, 610, 810, 60, { isStatic: true });

// setAngularVelocity(boxA, 0.0);
// // add all of the bodies to the world
// Composite.add(engine.world, [boxA, boxB, ground]);

// export const runSim = () => Runner.run(runner, engine);
