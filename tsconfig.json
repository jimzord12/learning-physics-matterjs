{
  "compilerOptions": {
    "target": "es6", // 🎯 Target modern JS features (ES6+)
    "module": "none", // 🌐 No module system (global mode)
    "lib": ["dom", "es6"], // 🌎 Access DOM + ES6 APIs
    "allowJs": true, // 📂 Allow JS files in the project
    "checkJs": true, // ✅ Enable type-checking for JS files
    "strict": true, // 🏹 Enable all strict type-checking options
    "noImplicitAny": true,
    "esModuleInterop": true, // 🔄 Ensure compatibility with CommonJS/ESM
    "skipLibCheck": true, // 🚀 Skip checking library declaration files for faster builds
    "types": ["p5", "matter-js", "stats"], // ⚠️⚠️⚠️ Explicitly include global type definitions
    "baseUrl": ".", // 📁 Resolve paths relative to project root
    "noEmit": true // 💡 Don't output compiled JS (useful for pure type-checking)
  },
  "include": ["src/**/*.js", "*.ts"] // 🎨 Include all JS/TS files & global declarations
  // "exclude": ["node_modules"] // 🚫 Ignore dependencies
}
